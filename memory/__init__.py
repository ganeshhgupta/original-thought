"""
memory/ — Persistent and in-session memory for the NOVA Reasoning System.

Modules:
  failure_store.py — SQLite-backed persistent failure memory across sessions.
  scratchpad.py    — In-session structured working memory (axioms, established
                     results, assumptions, conjectures, dead ends).
"""
